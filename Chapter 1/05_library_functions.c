#include <stdio.h>
int main(){
    int i = 10;
    printf("This is %d\n", i);
    // &d is for integers
    // %f is for real values (floating point numbers)
    // %c is for characters
    return 0;
}